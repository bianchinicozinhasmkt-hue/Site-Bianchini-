# dependencies
/node_modules
node_modules/

# next.js
/.next/
.next/
/out/
out/
next-env.d.ts

# production
/build
dist/

# artefato de release manual (Hostinger) — gerado localmente, não versionado
/release/

# typescript
*.tsbuildinfo

# coverage / testes
coverage/

# arquivos temporários e backups locais
*.tmp
*.temp
*.bak
*.old

# env
.env
.env.local
.env.development.local
.env.production.local
.env*.local

# logs
*.log
.next-dev.*.log
npm-debug.log*

# claude
.claude/settings.local.json

# editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
*.swp

# perfis temporários do Edge headless, criados pelos scripts de captura via CDP
# (scripts/*.mjs sobem o navegador com --user-data-dir apontando para cá).
# São milhares de arquivos do navegador, não do projeto.
.tmp-edge-cdp/
.tmp-edge-cdp*/

# artefatos do pacote de produção (scripts/gerar-pacote-hostinger.mjs)
# São gerados a partir do build e não são versionados: o que é versionado é o
# script que os recria. Regerar com `npm run pacote:hostinger`.
/deploy-hostinger/
/bianchini-hostinger.zip
